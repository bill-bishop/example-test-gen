{
  "name": "example-test-gen",
  "version": "1.0.0-prerelease-2026-04-01",
  "description": "Generate test files from @example snippets in your code",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "example-test-gen": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "remove-test-files": "node scripts/remove-test-files.js",
    "generate": "npx example-test-gen --config=vitest",
    "docs": "typedoc src/index.ts --out docs/generated",
    "pretest": "npm run remove-test-files && npm run generate",
    "test": "vitest run"
  },
  "keywords": [
    "test",
    "example",
    "jsdoc",
    "generate",
    "snippet"
  ],
  "files": [
    "dist/"
  ],
  "dependencies": {
    "fast-glob": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "typedoc": "^0.28.18",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

# Self-Testing (Dogfooding)

## Description

Library generates and runs its own tests via `@example` annotations.

## Original Requirement

**META01**: Self-Testing - Library generates and runs its own tests via `@example` annotations (dogfooding)

## Status

- User Reviewed: [x]
- Tests Completed: [ ]
- Implemented: [ ]

## Notes

This meta-feature involves using the library itself to generate tests from its own source code. The generated tests should be output to `tests/` directory.

This is currently not implemented but represents a key milestone for demonstrating the tool's capabilities on itself.
